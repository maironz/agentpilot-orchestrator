# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# pytest / coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Backups generati da rgen
.github/.rgen-backups/

# Secrets e file locali
.env
.env.*
*.local

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# SQLite (interventions db nei progetti target)
*.db
*.sqlite

# Claude Code memory (locale, non condiviso)
.claude/

# Continue / local agent state (locale, non condiviso)
.continue/
.continuerules

# Tracciatura discussioni (locale, non condiviso)
.discussioni/

# Piani locali personali (non condivisi)
.github/plans-local/

# Cache locale fallback update check (non condivisa)
.github/.remote_version_cache.json

# Package privati locali (non condivisi)
.private-packages/
